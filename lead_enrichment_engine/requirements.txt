pandas          # DataFrame processing for all pipeline stages
requests        # HTTP calls to Apollo, ZeroBounce, Apify, Clearbit, etc.
python-dotenv   # Reads API keys from the .env file
tenacity        # Automatic retry with exponential back-off for API calls
rapidfuzz       # Fuzzy string matching (used internally by cleaner)
pyarrow         # Fast Parquet read/write for checkpoint files
tqdm            # Progress bar shown during enrichment
openpyxl        # Read/write Excel .xlsx files (input conversion + write-back)
dnspython       # DNS MX record lookups — validates email domains, infers true email domain from MX host
sqlalchemy      # PostgreSQL input/output support (optional — only needed if PG_CONNECTION_STRING is set)
psycopg2-binary # PostgreSQL driver used by SQLAlchemy
fastapi         # Web framework for the app UI
uvicorn         # ASGI server to run FastAPI
python-multipart # Required by FastAPI for file upload / form handling
beautifulsoup4  # HTML parser for Google search result scraping
lxml            # Fast HTML/XML parser backend for BeautifulSoup
